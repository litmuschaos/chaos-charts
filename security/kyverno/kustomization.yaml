resources:
  - allow-capabilities.yaml
  - allow-host-paths.yaml
  - allow-privilege-escalation.yaml
  - restrict-volumes.yaml
  - allow-host-namespaces.yaml
  - allow-privileged-containers.yaml
  - allow-selinux.yaml
  - require-user-groups.yaml
patches:
  - patch: |-
      - op: replace
        path: /spec/validationFailureAction
        value: enforce
    target:
      kind: Policy
